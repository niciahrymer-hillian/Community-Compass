# Community Compass — Python FastAPI service (HomeMatch map/AI + Future-Path caseworker)
fastapi>=0.115
uvicorn[standard]>=0.30
sqlalchemy[asyncio]>=2.0
aiosqlite>=0.20            # SQLite async driver (local dev)
asyncpg>=0.30             # PostgreSQL async driver (production)
pydantic>=2.7
pydantic-settings>=2.3
email-validator>=2.1      # pydantic EmailStr
pyjwt[crypto]>=2.9        # Supabase JWT verification (JWKS needs cryptography)
cryptography>=42          # Fernet PII encryption + JWKS public keys
httpx>=0.27               # AI provider calls + ASGI test client
feedparser>=6.0           # HUD/DSHA RSS newsfeed (HomeMatch news_service)

# --- tests ---
pytest>=8.2
pytest-asyncio>=0.23
